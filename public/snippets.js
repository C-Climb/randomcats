document.onkeypress = (e) => {
    if(e.keyCode === 32){
        // logic here
        console.log(`${e.keyCode} has been clicked`)
    } else if(e.keyCode === 70){
        // logic here
    } else {
        return;
    }
};

// probably wont stay but we'll see how im feeling

